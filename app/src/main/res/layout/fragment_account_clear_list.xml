<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/my_gray_color"
    tools:context=".MainActivity">


    <LinearLayout
        android:id="@+id/clear_list_foot_button"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:padding="0dp"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true">

        <ImageButton
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/close_button"
            android:background="@color/my_gray_color"
            android:id="@+id/ib_clear_list_close" />

        <TextView
            android:id="@+id/tv_clear_list_close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="清空列表"
            android:textSize="10sp"
            android:textColor="@color/my_orange_color"

            />
    </LinearLayout>


    <ListView
        android:layout_above="@+id/clear_list_foot_button"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/clear_listView"
        android:layout_gravity="center_horizontal"
        android:footerDividersEnabled="false" />

</RelativeLayout>
